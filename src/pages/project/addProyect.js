// Add project to the sidebar

function addProject(projectName) {
    const projects = document.getElementById('projects');
    const project = document.createElement('div');
    project.classList.add('sidebar-line');
    //...
}